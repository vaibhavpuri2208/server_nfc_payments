<h1>Your Current Credit</h1>

<table>
  <tr>
    <th>Amount</th>
  </tr>

<% @credits.each do |credit| %>
  <tr>
    <td><%= credit.amount %></td>
    
    <td>  <%= link_to 'Add Credit', new_credit_path %></td>


  </tr>
<% end %>
</table>

<br />

<%= render 'add_credit' %>
<%= render 'customer_details' %>
<%= render 'card_details' %>

