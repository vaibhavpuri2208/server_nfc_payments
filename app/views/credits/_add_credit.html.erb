<%= form_for(@new_credit, :remote=>true) do |f| %>
  <% if @new_credit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_credit.errors.count, "error") %> prohibited this credit from being saved:</h2>

      <ul>
      <% @new_credit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br />
    <%= f.number_field :amount %>
  </div>
  
  <div class="actions">
    <%= f.submit "Authorise & Add" %>
  </div>
<% end %>